pose_tf_node:
  ros__parameters:
    # /pose is the converted raw pose; /pose/calibration is LiDAR-calibrated.
    tf_source: "/pose"
    # Base-to-LiDAR longitudinal offset applied only to calibrated TF.
    lidar_offset_x_m: -0.027
