visualizer:
  ros__parameters:
    enable_sim_gt: true
    sim_state_url: "http://localhost/sim/api/state"
    sim_poll_rate_hz: 20.0
    sim_timeout_sec: 0.05
    sim_retry_sec: 2.0
    # path_planning.yaml의 obstacle_inflation_radius_m와 동일하게 유지한다.
    obstacle_inflation_radius_m: 0.20
